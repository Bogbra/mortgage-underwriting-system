# Copy to .env.local. These are read server-side only (no NEXT_PUBLIC_
# prefix) — see lib/api.ts. Values below match the backend's own .env.example
# defaults for local development.

BACKEND_API_URL=http://localhost:8000
BACKEND_API_TOKEN=dev-local-token
BACKEND_REVIEWER_TOKEN=dev-reviewer-token

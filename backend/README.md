# underwriting

Multi-agent mortgage underwriting engine. See the repository root `README.md`
for the full project overview; this package contains the Python backend
(domain logic, LangGraph agent workflow, RAG policy retrieval, and the
FastAPI service).
